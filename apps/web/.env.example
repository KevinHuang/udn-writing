# Gemini API 金鑰（選用）。
# 沒有也能跑完整個流程 —— 批改會走 services/simulatedGrading.ts 的模擬批改。
# 會停用的只有看圖出題、手寫稿 OCR、產生評分規準這幾項。
#
# 使用方式：把這個檔複製成 .env.local 並填入金鑰。.env.local 不會進版控。
#
# ⚠️ 目前金鑰是被 Vite 的 define 直接替換進前端 bundle 的，
#    打開 devtools 就看得到。正式上線前必須把 Gemini 呼叫移到後端。
GEMINI_API_KEY=
